#include "cuda.cuh"


uint64_t cuda_productofdifferences(const unsigned char *x, const size_t n) {
    return 0;  // todo!
}

size_t cuda_removefactors(const int *input, const size_t n, const int divisor, int *output) {
    return 0;  // todo!
}

void cuda_chromaticaberration(const unsigned char *input, unsigned char *output, const size_t width, const size_t height) {
    // todo!
}
